let getChar = prompt("Enter character :");
// gets its Unicode value (ASCII code) using .charCodeAt(0)
//charCodeAt(0) means "get the character code of the first character".
let str =  getChar.charCodeAt(0); 
console.log(str)